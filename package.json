{
  "dependencies": {
    "npm-run-all": "^4.1.5"
  },
  "scripts": {
    "client": "cd client && npm run dev",
    "server": "cd server && npm start",
    "dev": "npm-run-all --parallel client server"
  }
}
